# Requiere Python >= 3.10

# Dependencias de runtime (proyecto actual)
numpy>=1.24,<3.0
pandas>=2.0,<3.0
odrpack>=0.5,<0.6
uncertainties>=3.2,<4.0
matplotlib>=3.8,<4.0
SciencePlots>=2.1,<3.0

# Dependencia de desarrollo (tests)
pytest>=8.0,<9.0

# Compatibilidad legacy (no requerida en la version actual)
# Solo para versiones antiguas del proyecto que usaban scipy.odr:
# scipy>=1.10,<1.16
